| Phase | Area | Current implementation on `server/code` | Planned implementation | Priority | Acceptance check |
|---|---|---|---|---|---|
| 1 | Secret management | MongoDB connection credentials are present as fallback values in tracked Python source files. | Remove every embedded credential, require environment variables at startup, add a safe `.env.example`, and rotate exposed credentials. | Critical | Application refuses to start without required variables; repository secret scan returns no credentials. |
| 1 | MongoDB read/write separation | Exercise reads and writes both use `MONGO_URI`; `MONGO_READ_URI` is not used by the active exercise handler. | Use `MONGO_READ_URI` for document and watermark reads and `MONGO_URI` for persistence and watermark writes, with explicit validation of both. | High | Integration tests prove reads and writes use their intended connections. |
| 1 | Current product scope | Dynamo is fetched, parsed, included in new-data decisions, and allowed to advance the shared timestamp even though Dynamo is not currently required. | Remove Dynamo from the active synchronization path while preserving any existing stored Dynamo section; keep Dynamo as a separate future implementation. | High | A ForceDeck/ForceFrame sync makes no Dynamo API call and does not modify cached Dynamo data. |
| 1 | Synchronization watermarks | One `lastRecordedUtc` value is used for ForceDeck, ForceFrame, and Dynamo. A newer result from one product can move every product forward. | Add independent `syncState.forceDeck.watermarkUtc` and `syncState.forceFrame.watermarkUtc`, with temporary fallback to legacy `lastRecordedUtc`. | Critical | Updating one product never changes the other product's effective watermark. |
| 1 | Persistence ordering | The WebSocket flow ignores the result of `push_exercise_data()` and can advance `lastRecordedUtc` after a failed database write. | Advance a product watermark only after its parsed data is confirmed stored; return an explicit failure when persistence or watermark writing fails. | Critical | A simulated MongoDB write failure leaves every watermark unchanged. |
| 1 | Cross-product data preservation | `push_exercise_data()` replaces the complete `savedResponse.data` object. A one-product response can remove the other product's stored data. | Merge only non-empty returned product sections and preserve all unreturned product sections and flags. | Critical | ForceFrame-only persistence leaves the existing ForceDeck section byte-for-byte unchanged, and vice versa. |
| 1 | Historical session preservation | Incremental parsed output can replace the stored history for the same product instead of merging individual tests/sessions. | Store and merge records by stable VALD test ID; retain prior sessions, deduplicate repeated results, and recalculate ordered session output safely. | Critical | Synchronizing one new test retains all old tests and produces no duplicate after the same payload is replayed. |
| 1 | Timestamp precision | The fetch decision ignores timestamp differences of one second or less. A technically newer VALD record can be rejected. | Preserve full UTC precision and use test-ID idempotency instead of a one-second timestamp tolerance. | High | A record newer by less than one second is processed once and only once. |
| 1 | Failure versus no-data result | Broad exceptions are converted to `{}`, so authentication, network, parsing, and API failures can be reported as "no new data." | Introduce explicit outcomes for `new_data`, `no_data`, and `failed`; propagate safe error details and retain full server diagnostics. | High | API timeout and genuine HTTP 204 produce different responses, logs, and retry behavior. |
| 1 | Patient-to-VALD association | `associatedValdId` is an array, but the processor silently uses only the first element. | Validate that exactly one active association is selected, reject ambiguous mappings, and log the selected mapping. | High | Zero, one, and multiple-association cases have deterministic tested behavior. |
| 1 | Cached API contract | The cached response is organized by exercise and session date but does not retain stable VALD test IDs; WebSocket success values are strings such as `"True"` instead of booleans. | Define and version a validated response model, retain source test IDs internally, and return consistent boolean/status/error fields. | High | Contract tests validate the response schema and every cached session is traceable to a VALD test ID. |
| 2 | VALD pagination | ForceDeck and ForceFrame collection requests process a single response even though VALD limits and paginates returned tests. | Follow each product's documented pagination cursor/page information until HTTP 204 or the final page, with repeated-cursor protection. | Critical | A fixture containing more than one API page imports every test exactly once. |
| 2 | HTTP reliability | ForceDeck profile, test, and trial requests have no timeout; device clients have inconsistent timeout and retry behavior. | Apply bounded connect/read timeouts, retry only transient failures with exponential backoff and jitter, and respect rate-limit responses. | High | Timeout, 429, and transient 5xx tests terminate predictably without duplicate persistence. |
| 2 | VALD configuration | Tenant and Australia-East API endpoints are hardcoded in active fetch modules. | Require validated tenant and region configuration and build product endpoints from an approved region map. | High | Startup reports the selected region safely; invalid or missing configuration fails before synchronization. |
| 2 | OAuth token request safety | An in-memory per-process token cache already exists, but the token HTTP request has no timeout and the global cache is not explicitly protected against concurrent refreshes. | Retain the existing expiry-aware cache, add a bounded timeout, and make refresh synchronization safe for concurrent callers. | Medium | Concurrent calls reuse one valid token, a stalled token endpoint times out, and refresh occurs once near expiry. |
| 2 | Async request handling | The async WebSocket directly calls synchronous PyMongo and `requests` functions, so one slow patient synchronization can block the application's event loop. | Move blocking work to a bounded worker pool or job queue, or adopt supported asynchronous clients, while keeping per-patient concurrency controlled. | Critical | A slow VALD request does not prevent unrelated health checks or WebSocket connections from responding. |
| 2 | VALD request amplification | Every patient sync downloads the tenant profile list; ForceDeck trials and ForceFrame metric details are then fetched sequentially once per test. | Replace the tenant-wide profile check with a direct profile lookup or trusted validated mapping, and fetch required per-test details with bounded concurrency or supported bulk endpoints. | High | Request-count tests show no tenant-wide profile download per patient and bounded per-test calls remain within rate limits. |
| 2 | MongoDB connections | A new `MongoClient` is opened and closed for most handler operations. | Use application-lifecycle Mongo clients with connection pooling, server-selection timeouts, and clean shutdown. | Medium | Load tests show stable connection counts and lower database connection overhead. |
| 2 | Database schema and indexes | Runtime code assumes document structure and uniqueness; the repository does not enforce required schema or indexes. | Add application-level schema validation and, after auditing duplicates, create indexes for unique `stanceId`, association lookup, and required sync queries. | High | Duplicate audit passes, index creation is repeatable, and invalid document shapes fail safely. |
| 2 | MongoDB document growth | Identity mapping, every processed session, flags, and synchronization state are stored in one patient document and rewritten as one cached object. There is no document-size guard. | Define retention and size limits; if long-term history can approach MongoDB's document limit, store normalized tests in a separate collection and materialize the frontend summary. | High | Long-history load tests remain below an approved size threshold and adding one test does not require unsafe full-history replacement. |
| 2 | Concurrent synchronization | WebSocket requests and batch workers can process the same patient simultaneously and overwrite each other's data or watermarks. | Add a per-patient distributed lease or optimistic version check and make persistence idempotent. | Critical | Two concurrent sync attempts cannot lose data or regress a watermark. |
| 2 | Batch retry/resume | Failed patients are included in the batch worker's `already_done` set, so restarting the same chunk does not retry them. | Track retryable and terminal failures separately, apply bounded retries, and generate a clear reconciliation report. | High | Restarting a chunk retries retryable failures without repeating successful patients. |
| 2 | Batch concurrency and API limits | Six fixed workers run regardless of tenant rate limits or workload size. | Make concurrency configurable and apply a shared rate limiter with backpressure. | Medium | Batch processing stays within configured request limits and can be tuned without code changes. |
| 2 | Batch partition consistency | Every worker independently reloads an unsorted MongoDB cursor and calculates its own slice, so collection changes or differing natural order can cause overlap or omissions. | Create one deterministic, sorted run manifest/snapshot and assign immutable patient-ID chunks to workers. | High | All workers together process every manifest ID exactly once even if the source collection changes during the run. |
| 3 | Production endpoint security | Debug, inspection, cron-status, and batch-control routes are available without authentication; `/stop-batch` can terminate worker processes. | Remove production-only debug routes or protect operational routes with strong authentication, authorization, and audit logging. | Critical | Anonymous requests cannot inspect patient data, trigger diagnostics, or stop batch jobs. |
| 3 | WebSocket and CORS security | CORS allows every origin while credentials are enabled, and the WebSocket accepts an unvalidated payload without authentication. | Restrict configured origins, authenticate connections, validate request models, and enforce message-size and rate limits. | High | Unauthorized origins/users are rejected and malformed or oversized messages fail safely. |
| 3 | Health and readiness | `/health` reports a static healthy response without testing MongoDB or required configuration. | Separate liveness from readiness and verify required configuration plus safe MongoDB connectivity in readiness checks. | Medium | Readiness becomes unhealthy when required dependencies are unavailable while liveness remains meaningful. |
| 3 | Logging and observability | Logs are mainly free-form, failures are sometimes swallowed, and there are no consistent request/run correlation IDs or sync metrics. | Add structured logs, correlation IDs, per-product durations/counts, failure categories, watermark movement, and alertable metrics without logging secrets. | High | One patient sync can be traced end-to-end and dashboards distinguish no-data, failure, and success. |
| 3 | Docker deployment definition | The checked-in Compose file defines the main app and match updater but not the VALD exercise WebSocket service; separate shell scripts launch the exercise container. | Either add an explicit Compose exercise service or formally standardize the separate deployment path, including health checks, environment validation, restart policy, and documentation. | High | A clean documented deployment starts every required service and passes health/readiness checks. |
| 3 | Deployment port allocation | Deployment scripts reuse host port `8091` for both the main app's green candidate and the VALD exercise service; the standalone exercise script also maps host port `8090`, which is assigned to the updater. | Assign non-overlapping ports, validate availability before stopping services, and keep a single authoritative port map. | Critical | Deployment tests start the main, updater, candidate, and exercise containers together without bind conflicts. |
| 3 | Parser configuration assets | The three `metric-pool` CSV files and three `graph-pool` images are now tracked, and the parser currently loads 2,756 graph mappings plus 1,919 new-metric mappings. Missing or malformed assets still degrade to warnings/fallbacks without schema or version validation. | Validate required asset presence and columns during build/startup, version the configuration, and define whether invalid assets should fail readiness or use an explicit fallback. | Medium | A clean container loads the expected versioned assets and a malformed CSV produces the configured readiness failure or fallback signal. |
| 3 | Metric units | ForceFrame impulse and repetition metrics currently fall back to `N` after their specialized mappings were removed on the server branch. | Restore explicit `N·s` and `reps` mappings and cover unit selection with parser regression tests. | Medium | Known impulse and repetition fields return the correct units. |
| 3 | Automated tests and CI | The server branch has diagnostic/manual scripts but no committed regression suite covering synchronization and persistence safety. | Add unit, integration, parser-fixture, concurrency, and failure-path tests; run linting, compilation, secret scanning, and tests in CI. | High | CI blocks changes that reintroduce history loss, shared-watermark errors, secret exposure, or parser regressions. |
| 3 | Container hardening | The Docker image runs as root and retains build tools in the runtime image. | Use a smaller multi-stage image, run as a dedicated non-root user, add a container health check, and scan the image and pinned dependencies. | Medium | The production container runs non-root, contains no compiler toolchain, and passes vulnerability and health checks. |
| 3 | Scheduler/runtime consistency | The cron setup script expects a release-local `one-view/bin/python3`, while the dedicated exercise deployment archive explicitly excludes `one-view`; infrastructure hostnames, key paths, and ports are duplicated across scripts. | Run batch jobs through the deployed image or a documented virtual environment and centralize environment-specific deployment configuration. | High | A clean server can install and execute the scheduler from one documented release without manually created paths. |
| 3 | Legacy and duplicate code | Multiple MongoDB helpers, debug utilities, dead reset logic, and overlapping synchronization functions implement similar behavior differently. | After regression coverage is established, select one active path, migrate callers, remove confirmed dead code, and document retained operational utilities. | Medium | Static search finds one authoritative implementation per responsibility and all supported entry points pass tests. |
| 4 | Controlled data migration | Existing documents use the shared legacy timestamp, while cached response structure and size are not schema-versioned or validated by the application. | Back up affected documents, audit duplicates, introduce the new schema compatibly, validate with approved test users, and then perform a staged historical reprocess. | Critical | Before/after reconciliation confirms no patient history was lost and product counts/watermarks are explainable. |
| 4 | Production rollout | Deployment scripts exist, including a blue-green flow for the main app, but the exercise-service deployment stops/removes the current container before validation and has no automatic health-based rollback; cron logs are retained for seven days. | Add staging validation, approved patient canaries, exercise-service health gates, centralized logs with an agreed retention period, immutable release metadata, and automatic rollback. | Critical | Canary users reconcile successfully before full rollout, exercise deployment rolls back on failed health checks, and logs identify the exact deployed release. |
